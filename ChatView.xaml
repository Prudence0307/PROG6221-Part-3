<UserControl x:Class="CyberChatBot.App.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Chat Messages -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
            <ScrollViewer>
                <ListBox x:Name="MessagesListBox" ItemsSource="{Binding Messages}" Background="White">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="5" Padding="10" CornerRadius="8" 
                                    Background="{Binding IsUser, Converter={StaticResource UserToBackgroundConverter}}"
                                    HorizontalAlignment="{Binding IsUser, Converter={StaticResource UserToHorizontalAlignmentConverter}}">
                                <TextBlock Text="{Binding Text}" TextWrapping="Wrap" FontSize="14"/>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </Border>

        <!-- Input Area -->
        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="InputTextBox" Grid.Column="0" Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}" 
                     FontSize="14" Padding="10" KeyDown="InputTextBox_KeyDown"/>

            <Button Grid.Column="1" Content="Send" Width="80" Height="40" Margin="10,0,0,0"
                    Command="{Binding SendMessageCommand}" Background="#2196F3"/>
        </Grid>

        <!-- Status -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="10" Margin="0,10,0,0" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="🤖" FontSize="18"/>
                <TextBlock Grid.Column="1" Text="{Binding BotStatus}" FontSize="13" Foreground="#666" Margin="10,0,0,0" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="{Binding Sentiment}" FontSize="13" Foreground="{Binding SentimentColor}" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>