<UserControl x:Class="CyberChatBot.App.Views.TaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Task Input -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="15" CornerRadius="5" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Add New Task" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                
                <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding NewTaskTitle}" FontSize="14" Padding="8" Margin="0,0,10,0" 
                         ToolTip="Enter task title"/>
                
                <Button Grid.Row="1" Grid.Column="1" Content="Add Task" Width="100" Height="35" 
                        Command="{Binding AddTaskCommand}" Background="#2ECC71"/>
                
                <TextBox Grid.Row="2" Grid.Column="0" Text="{Binding NewTaskDescription}" FontSize="13" Padding="8" Margin="0,10,10,0"
                         ToolTip="Enter task description"/>
                
                <DatePicker Grid.Row="2" Grid.Column="1" SelectedDate="{Binding NewTaskReminder}" 
                            FontSize="13" Margin="0,10,0,0" Width="140" VerticalAlignment="Center"/>
            </Grid>
        </Border>
        
        <!-- Task List -->
        <ListBox Grid.Row="1" ItemsSource="{Binding Tasks}" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="10" Margin="2" Background="#FAFAFA" CornerRadius="3">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <CheckBox Grid.Column="0" IsChecked="{Binding IsCompleted}" Margin="0,0,10,0"
                                      Command="{Binding DataContext.ToggleTaskCommand, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}}"
                                      CommandParameter="{Binding}"/>
                            
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14" 
                                           TextDecorations="{Binding IsCompleted, Converter={StaticResource BoolToStrikethroughConverter}}"/>
                                <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#666" 
                                           Visibility="{Binding Description, Converter={StaticResource StringToVisibilityConverter}}"/>
                                <TextBlock Text="{Binding ReminderDate, StringFormat='Reminder: {0:yyyy-MM-dd HH:mm}'}" 
                                           FontSize="11" Foreground="#999"/>
                            </StackPanel>
                            
                            <Button Grid.Column="2" Content="✓" Width="30" Height="30" Background="#4CAF50" 
                                    Command="{Binding DataContext.ToggleTaskCommand, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}}"
                                    CommandParameter="{Binding}" Margin="5,0"/>
                            
                            <Button Grid.Column="3" Content="✕" Width="30" Height="30" Background="#F44336"
                                    Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}}"
                                    CommandParameter="{Binding}" Margin="5,0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
