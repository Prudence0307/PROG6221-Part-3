<UserControl x:Class="CyberChatBot.ActivityLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:YourNamespace">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#9B59B6" Padding="15" CornerRadius="5" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="📋" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="Activity Log" FontSize="20" Foreground="White" FontWeight="Bold"/>
                </StackPanel>

                <Button Grid.Column="1" Content="Clear Log" Background="#C0392B" Foreground="White"
                        Command="{Binding ClearLogCommand}" Margin="0,0,10,0"/>
            </Grid>
        </Border>

        <!-- Log Entries -->
        <ListBox Grid.Row="1" ItemsSource="{Binding LogEntries}" Background="White" 
                 BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="12" Margin="2" Background="#FAFAFA" CornerRadius="3">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Timestamp, StringFormat='{0:yyyy-MM-dd HH:mm}'}" 
                                       FontSize="12" Foreground="#888" Margin="0,0,15,0"/>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Action}" FontWeight="SemiBold" FontSize="14"/>
                                <TextBlock Text="{Binding Details}" FontSize="12" Foreground="#666" 
                                           Visibility="{Binding Details, Converter={StaticResource StringToVisibilityConverter}}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Status -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="10" Margin="0,10,0,0" CornerRadius="5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📊" FontSize="18"/>
                <TextBlock Text="{Binding LogCount}" FontSize="13" Foreground="#666" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
````````

````````
using System;
using System.Globalization;
using System.Windows;
using System.Windows.Data;

public class StringToVisibilityConverter : IValueConverter
{
    public object Convert(object value, Type targetType, object parameter, CultureInfo culture)
    {
        var str = value as string;
        return string.IsNullOrWhiteSpace(str) ? Visibility.Collapsed : Visibility.Visible;
    }

    public object ConvertBack(object value, Type targetType, object parameter, CultureInfo culture)
    {
        throw new NotImplementedException();
    }
}